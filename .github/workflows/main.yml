name: Blog

on:
  push:
    branches:
      - master
  issues:
    types:
      [
        opened,
        closed,
        edited,
        milestoned,
        labeled,
        unlabeled,
        reopened,
        demilestoned,
      ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [19.x]

    steps:
      - uses: pnpm/action-setup@v2
        with:
          version: latest
      - uses: actions/checkout@v3
        with:
          repository: FeijiangHan/Issue-Blog-With-Github-Action
          ref: source
      - name: Build
        env:
          GITHUB_TOKEN: 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
        run: |
          wget https://raw.githubusercontent.com/FeijiangHan/FeijiangHan.github.io/main/.github/workflows/cv.md -O ./src/cv.md
          wget https://raw.githubusercontent.com/FeijiangHan/FeijiangHan.github.io/main/.github/workflows/custom.js -O ./src/.vuepress/custom.js
          pnpm i
          pnpm action
          pnpm build
          pnpm run deploy
          
